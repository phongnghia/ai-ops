"""LLM client layer package.

Provides the ``LLMClient`` abstraction and its concrete implementation that
talks to the LiteLLM gateway through an OpenAI-compatible API. The abstraction
lets providers be swapped without changing ``core`` logic.
"""
